Corrected the Apple Silicon GPU story: QEMU 9.2+ carries virtio-gpu Venus and macOS virglrenderer builds work (KosmicKrisp/MoltenVK), so libkrun was never needed -- one VMM on the Mac, not two. Replaced the Venus-guest-driver plan with a purpose-built virtio-mlaccel device (typed op ring, Metal/CUDA host backends), hundreds of guest lines instead of tens of thousands. Made explicit that M1-M5 need no GPU at all, retiring the risk that Apple opacity gates the project. Added architecture s.7.1b assessing Asahi Linux (excellent, bare-metal-only, Linux-DRM-bound, GPL -> reference not dependency). Added design.md s.9 'How you interact with MLOS': virtio-console into mlsh inspector, host mlos CLI, gdb stub, future metrics TUI. Updated plan/status parked lists and decision log; mlsh added to the mlos-objects saga.