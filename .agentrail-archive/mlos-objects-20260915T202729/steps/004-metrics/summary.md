mlos-metrics: per-class fault and byte counters, registered/resident totals, and Rm, wired into Manager so the manager counts what it services. Design points each tested: per-class because 'a thousand faults on scales vs ten on experts' inverts depending on whether you count events or bytes, so both are kept; Rm in parts per thousand because a kernel has no floating point and the ratio matters more than the precision; Rm is None with nothing registered because an absent denominator is unanswerable, not zero; resident bytes signed so the first eviction does not look like a leak; saturating so a long run cannot report less than a short one. Added ObjectClass::ALL and index() so per-class accounting needs no second list that could drift. 7 new tests. Wrote one test that was wrong and fixed the test rather than the code (two i64::MAX additions reach 2^64-2, not u64::MAX). SCOPE: the step said 'readable from mlsh'; deferred to 005 where the synthetic model gives it something to report -- a shell command reading zeros demonstrates nothing. Gate green apart from the standing mlos-cli exception: sw-checklist 105 passed / 0 failed.