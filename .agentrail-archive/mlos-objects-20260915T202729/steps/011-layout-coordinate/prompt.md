Close the loop with the other three repos, and leave behind an artifact they can
build against without running MLOS.

Commit a conforming sample of each emitted file under examples/viz/ --
storage-layout.json and runtime-layout.json, generated by the real emitters, not
hand-written -- and report their sha256 alongside the MLOS commit, so sw-mlpl and
demo-extensions can pin exactly what they tested against. That is precisely the
unblocking artifact the demo-extensions agent asked sw-tos for; MLOS should not
make them ask twice.

Then relay, in writing, what MLOS provides and needs:

  To sw-mlpl -- the vocabulary. MLOS's region_kind/region_owner/region_state
  vocabularies are NOT sw-tos's: kinds are object classes (weight tile, KV block,
  expert, activation, context) plus kernel/arena/free/padding; owners are models
  and sessions, not programs; states are resident/evicted/never; and there is a
  region_tier of five values. Their palette covers the UNION of producer
  vocabularies, so hand them the exact lists rather than a description of them.
  Also tell them MLOS emits padding as its own kind (as sw-tos does) and that
  region_next_use is a string with three shapes.

  To demo-extensions -- the extra thing the MLOS data wants that a storage map
  does not: two spaces shown side by side with edges between them (tile on disk
  -> arena placement), and a time dimension from the step-010 event stream.
  Whether set_boxes/set_highlight as specified already covers it is their call;
  ask rather than assume.

  To sw-tos -- what MLOS did differently and why, so the contract's second
  producer informs the first rather than silently diverging.

Update docs/status.md: what the emitters produce, what is real and what is
still synthetic, and the known gaps. Anyone reading it should be able to tell
whether a picture they are shown is of a running system or of a build artifact.
