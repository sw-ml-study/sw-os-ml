mlos CLI: build / run [hvf|tcg] / run --capture N / run --debug / doctor, replacing scripts/boot.sh and scripts/image.sh. run is interactive with mlsh on the other end; --capture is the headless counterpart for tests; --debug opens the gdb stub. doctor asks run for its accelerator list so the two cannot drift. Writing doctor found two bugs IN doctor (probe returned only the first line, so list-shaped checks matched nothing past their first entry; and an untrimmed rustc --print sysroot made the objcopy path not exist; plus ffmpeg wants -version) -- a diagnostic that lies about what is missing is worse than none. TWO REAL BUGS found, one hiding the other: a stray -serial mon:stdio left in base_args made --capture pass two serial backends, so QEMU instantiated a second PL011 at 0x9040000 (visible as 'console spi 40'); underneath it, the DTB scanner took the LAST pl011@ node, which with two UARTs is the spare -- MLOS would print where nobody reads. Now takes the first, with a second fixture (qemu-virt-two-uarts.dtb, the tree that exposed it) testing it. /chosen/stdout-path is the exact answer but /chosen comes after the UARTs, so it needs end-of-walk resolution; recorded in docs/status.md rather than half-done. Retired 5 checklist warnings, mostly by table-driving doctor. Gate green: fmt clean, 21 test bins, clippy -D warnings clean on host and both bare targets, both builds ok, sw-checklist 71 passed / 0 failed / 0 warnings.