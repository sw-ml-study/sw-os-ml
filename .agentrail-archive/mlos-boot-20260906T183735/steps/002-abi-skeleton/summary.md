mlos-abi landed: no_std + forbid(unsafe_code), three modules (error, class, object). ObjectId pins the 8/16/16/16/8 class|model|layer|tensor|tile layout that ML-MMU gateware decodes; class() is fallible (zero is not a class, so an all-zero word is rejected) while fields() is infallible; fields returned as a group, mirroring how gateware latches and slices the word, which also keeps the module at 3 functions. ObjectClass carries the 8 classes with is_session_scoped() distinguishing KV/Activation (which reuse the model field as a session id). Error carries 8 codes with Refused documented as a normal admission-control outcome, not a failure. Six tests in tests/layout.rs pin the layout with a literal expected word and max-value field round-trips. mlos-abi is built for both bare targets in the gate so it cannot quietly grow a host dependency. Gate refinements: bare-target clippy needs --lib --bins (no test harness on bare metal); added k* aliases naming bare-target-capable crates explicitly; default-members now lists mlos-abi. Gate green: fmt clean, 6 tests pass, clippy -D warnings clean on host and both bare targets, both builds ok, sw-checklist 11 passed / 0 failed / 0 warnings.