virtio-console driver working over virtio-mmio, selected at boot from /chosen/bootargs console=hvc0; the entire boot including the mlsh prompt goes out through a virtqueue under both HVF and TCG, via mlos run --console virtio. New crates: mlos-virtio (regs/queue/console, modern v2 transport, synchronous split virtqueue) and mlos-console (Terminal enum choosing PL011 or virtio, preferring PL011 because it needs no setup). THREE FINDINGS: (1) QEMU virt defaults virtio-mmio to VERSION 1 so every slot probed as absent -- -global virtio-mmio.force-legacy=false is required; diagnosing it needed a raw register dump over the PL011. (2) QEMU assigns devices from the highest slot downward; the console was slot 31 of 32, vindicating probe-don't-assume. (3) DECISIVE: Virtualization.framework puts virtio on the PCI bus (CONFIG_VIRTIO_PCI), not MMIO -- so this does NOT close N2. VZ offers neither PL011 nor virtio-mmio. Closing N2 needs PCI ECAM + virtio-pci, which is mlos-pci that M6 already requires for GPU passthrough; N2 therefore moves to M6 rather than writing PCI twice. The driver is not wasted: virtio-mmio is what QEMU offers and is the provider transport M2 needs. efi-stub parked per instruction. Retired 7 of 8 checklist warnings, two by real relocation (device-tree string helper to mlos-fdt; virtio reporting to mlsh dev); reverted one split that moved the problem. Gate green apart from the standing mlos-cli exception: fmt clean, 24 test bins, clippy -D warnings clean everywhere, both builds ok, sw-checklist 80 passed / 0 failed.