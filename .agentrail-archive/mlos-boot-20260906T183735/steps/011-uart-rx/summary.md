MLOS accepts input. mlos-pl011 gained a receive path (split by direction: lib.rs the device, tx.rs polled transmit, rx.rs interrupt-driven receive incl. drain_echo), GICv3 gained the distributor path for shared interrupts with Gic::enable dispatching on interrupt number, and the UART's interrupt is discovered from the device tree with the +32 SPI rebasing tested against the real blob. Verified by vhs: keystrokes typed into a real pty are echoed back through PL011 RX -> SPI 33 -> distributor -> IRQ vector -> transmit. vhs is necessary rather than decorative -- a piped stdin is not a terminal, so the receive FIFO never sees keystrokes and this path cannot be exercised from a pipeline at all; the tape emits text as well as GIF so regressions fail a grep. Added scripts/image.sh after a stale image produced a confusing recording. Retired 3 checklist warnings + 1 clippy error by restructuring: pl011 split by direction, and fault formatting moved into mlos-trap-aarch64 (it is about Trap's registers, not kernel policy), which let the kernel's trap/handlers modules collapse into one. Gate green: fmt clean, 20 tests, clippy -D warnings clean on host and both bare targets, both builds ok, sw-checklist 56 passed / 0 failed / 0 warnings.