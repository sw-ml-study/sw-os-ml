Three TCG boot tests plus a GitHub Actions workflow running the full AGENTS.md gate on an aarch64 Linux runner. TCG not HVF, deliberately: it is deterministic, so a failing boot fails identically every run. Tests assert an ordered SEQUENCE rather than a set, because order is the boot sequence; separate tests for the PL011 and virtio paths (they share nothing below the banner); and one asserting the kernel image is not reported as usable memory, which is the gap step 006 left. Marked #[ignore] since each boots a VM for 8s; CI runs them with --ignored. FOUND A REAL BUG by running them in parallel: mlos run --capture used a fixed temp path, so concurrent captures clobbered each other and one test failed only when run alongside the others -- indistinguishable from a kernel that sometimes does not print. Log is now unique per process and removed after. Updated the AGENTS.md gate and docs/status.md. Gate green apart from the standing mlos-cli exception: fmt clean, 25 fast test bins + 3 boot tests, clippy -D warnings clean everywhere, both bare builds ok, sw-checklist 80 passed / 0 failed.