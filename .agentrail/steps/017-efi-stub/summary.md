PARKED BY DECISION -- no code written, and none should be until something needs it. A dual-format Image/PE-COFF binary buys UEFI boot and VZEFIBootLoader. Neither is needed: VZLinuxBootLoader takes the raw image MLOS already produces, and Virtualization.framework cannot show output at all until it has a PCI console (M6), so a boot path is not what is missing there. Step 016 measured both facts. Recorded as parked in docs/status.md; unpark when something actually needs UEFI firmware services. Reward 0: the step is closed, not achieved.