First interrupt. mlos-gic-aarch64 (distributor/redistributor/CPU-interface, GICv3 only) plus the ARM generic timer in mlos-hal-aarch64, driven by a new IRQ vector path that saves caller-saved regs + ELR/SPSR, dispatches, restores and erets. Timer ticks at 2 Hz and tracks wall clock (2s->4 ticks, 4s->8, 6s->12) on both TCG and HVF; step 009's deliberate fault retired. TWO FINDINGS: (1) QEMU virt gives a different interrupt controller per accelerator -- TCG defaults to GICv2 (arm,cortex-a15-gic, whose second reg range is a CPU interface not a redistributor), HVF gives v3 -- so boot.sh now pins gic-version=3 and the fixture was recaptured; (2) a device tree does not order a node's properties, and QEMU lists intc's reg BEFORE compatible, so compatible and reg are now captured independently and combined after the walk. Both caught by a test against the real blob rather than by a hang. Init order is load-bearing and quiet when wrong at every layer (ARE, redistributor wake, ICC_SRE_EL1 before other ICC_* are accessible, unmask last); PPIs live in the redistributor not the distributor; rearming the timer prevents a livelock and EOI prevents silence. Retired 4 checklist warnings + 1 clippy MSRV error in-step, the useful one collapsing three reg handlers into one and moving map-filling into Regions. Gate green: fmt clean, 19 tests, clippy -D warnings clean on host and both bare targets, both builds ok, sw-checklist 51 passed / 0 failed / 0 warnings.