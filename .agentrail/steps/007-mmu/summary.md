mlos-mmu-aarch64: identity map of 1 GiB level-1 blocks built from BootInfo regions (device below 1 GiB, normal for claimed RAM, invalid elsewhere so stray access faults), MAIR/TCR/TTBR0 programmed, SCTLR_EL1.M/C/I set. Verified on TCG and HVF, and at -m 512/2048/4096 where RAM spans 1/2/4 blocks, proving the map follows the device tree rather than an assumption. SCTLR_EL1.M is READ BACK and printed rather than asserted, and printed after the switch through the newly-mapped console, so the line proves both. Key correctness details: AF set in every descriptor (else access-flag fault with no handler); IPS read from ID_AA64MMFR0_EL1.PARange rather than assumed (a wrong value is architecturally unpredictable at the worst moment); MMIO mapped Device-nGnRE and non-executable. Deliberately not an impl of mlos_hal::PageTable -- a general mapper written before it has a caller is the wrong mapper; recorded as a known gap. Retired 2 sw-checklist warnings + 2 clippy errors in-step: regs::enable split into install/turn_on (also the honest shape: two separately-ordered barrier sequences), mlos_main's sequence moved into a kernel boot module, no-op TG0 term removed, missing SAFETY comment added. SCOPE CORRECTION: this step does NOT close the BootInfo kernel-range gap as I said it would -- mlos-hal-aarch64 is at the 4-module gate so doing it properly needs devicetree.rs moved to a sibling crate; inserted as 008-memory-map. Gate green: fmt clean, 13 tests, clippy -D warnings clean on host and both bare targets, both builds ok, sw-checklist 36 passed / 0 failed / 0 warnings.