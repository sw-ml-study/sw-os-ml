Confirmed step 004's hypothesis by probe (d00dfeed present at 0x4000_0000 under ELF boot, so QEMU places a DTB but skips the boot protocol for ELF), then fixed it properly: _start is now the 64-byte arm64 Image header branching to primary_entry in .text.entry, with ordered KEEPs in the linker script so the header sits at image offset 0. text_offset 0x200000 with the placement flag clear pins the load address to the 0x4020_0000 we link for; image_size (0x13000) is the memory footprint including .bss and stack, not the 10267-byte file size. Added __image_size to the linker script. boot.sh objcopies to a flat Image and boots that; the ELF stays for gdb symbols. Verified on TCG and HVF: x0 = 0x48000000, and the QEMU monitor independently shows 0xedfe0dd0 / totalsize 0x1000 there. Also satisfies step 010's need for an uncompressed raw arm64 image for Virtualization.framework. Gate green: fmt clean, 9 tests, clippy -D warnings clean on host and both bare targets, both builds ok, sw-checklist 26 passed / 0 failed / 0 warnings.