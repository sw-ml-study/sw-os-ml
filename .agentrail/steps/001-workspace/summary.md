Cargo workspace on Rust 2024: pinned toolchain (1.96.0 + both bare targets), linker scripts for aarch64 (base 0x40200000) and x86-64, mlos-kernel binary building for aarch64-unknown-none-softfloat and x86_64-unknown-none, build.rs resolving the linker script to an absolute path. Found and fixed a real bug: a plain extern "C" _start compiles to a stack push, and with the loader's undefined SP it faults to VBAR_EL1+0x200 before retiring any of our code -- reproduced under BOTH TCG and HVF, so it was not the hypervisor difference it first looked like. Entry point made naked; verified both accelerators hold at 0x40200004 in EL1h. Corrected the AGENTS.md gate: --workspace cannot include a no_std/no_main crate on the host, so the kernel is named with -p and an explicit --target. Gate green: fmt clean, cargo test ok, clippy -D warnings clean on both targets, both builds ok, sw-checklist 6 passed / 0 failed / 0 warnings.