MLOS boots. mlos-hal-aarch64 landed: naked _start (park secondaries via MPIDR_EL1.Aff0, set sp from __stack_top, zero .bss, branch to mlos_main with x0 preserved), PL011 console with Console + fmt::Write impls, early_console() as an explicitly labelled hardcoded-base crutch that step 005 deletes. Kernel prints a banner and halts; verified identical output under QEMU TCG (cortex-a72) and HVF (-cpu host). Added a 64 KiB boot stack to the linker script. Added scripts/boot.sh (serial to file, monitor to stdio -- the reverse wedges QEMU because quit lands on the guest console). FINDING handed to step 005: x0 is 0, not a DTB pointer; QEMU does build a DTB for -M virt but appears to skip the arm64 Linux boot protocol for ELF -kernel images. Hypothesis to confirm in 005: emit a raw arm64 Image with its 64-byte header, which is wanted anyway for Virtualization.framework in step 009. Rescoped this step before starting: split the original four-part step into 004-aarch64-entry / 005-dtb / 006-mmu via agentrail insert. Gate green: fmt clean, 9 tests, clippy -D warnings clean on host and both bare targets (x86-64 clippy caught an unused import the build did not), both builds ok, sw-checklist 26 passed / 0 failed / 0 warnings.