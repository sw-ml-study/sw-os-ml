Four new mlsh verbs make the object manager explorable rather than watchable:  acquires one tile and says whether it hit or faulted and what it cost;  lists tier, residency and use count per object;  reports occupancy, headroom and Rm; and  now takes a budget so the arena can be resized from the shell.  twice on one tile is the shortest demonstration of what an object table is for -- the second is free, and that tile's use count reads 2 against 1 for everything a sweep touched. mlos-synth now exposes the manager via a single  accessor instead of wrapping each question, so the shell can ask things the crate did not anticipate. Extracted mlos-line (a line buffer is not shell-specific, same reasoning as mlos-queue). Hoisted the 'no model registered' guard into dispatch before it became a third copy. NOTE: this step spent far too many rounds oscillating between a 5-function module and a 5-module crate, each fix trading one warning for the other; what worked was turning the help text into a const (as in mlos-cli) plus removing real duplication. Gate green apart from the standing mlos-cli exception: sw-checklist 115 passed / 0 failed, 27 test bins.