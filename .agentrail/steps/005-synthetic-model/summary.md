Gates G2 and G3 demonstrated from a running kernel via mlsh: model registers 136 objects / 144 KiB across three tiers (Cold behind a backing store, Archive behind recompute, resident in the arena); sweep faults 32 of 128 tiles in and stops at NoBudget; faults reports 33 faults by class, 33 KiB fetched, Rm 222 per mille, and the last fault naming model/layer/tensor/tile with its cost. FOUND AND FIXED A REAL BUG: the fault path allocated but never fetched -- service called prefetch, placed the object and marked it resident, so the next acquire was a hit returning whatever was in the arena before. Host tests passed because none read the bytes back. Arena now owns its storage so place() can hand a provider somewhere to write, and a test reads the pattern back. Removed prefetch from the fault path, which is correct: prefetch is what a policy does ahead of a fault, asynchronously. Arena is a quarter of the model deliberately -- 32 of 128 is the premise, not a shortfall, and these numbers are the baseline M3 must beat. Collapsed Backing and Recompute into one Tier type since they differed only in cost. New crates mlos-synth (model, tiers, statics) and three mlsh verbs. Gate green apart from the standing mlos-cli exception: sw-checklist 110 passed / 0 failed.