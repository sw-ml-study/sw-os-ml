Fixed a bug reported from a real session: pasting a README command with a trailing '# comment' into interactive zsh (where # is NOT a comment) passed the note as arguments, and mlos forwarded the first to QEMU -- the user saw 'invalid accelerator #' from a program they never typed. Now every argument is validated before anything runs: unknown accelerator, unknown option, missing --capture value, and unexpected positional arguments each get an accurate message and exit 1. The worst case was '--nope': the old parser skipped anything starting with a dash and silently launched an INTERACTIVE qemu with no terminal, hanging until killed. options() takes whether the command accepts a positional, so 'mlos doctor oops' reports an unexpected argument rather than a bad accelerator. README fixed to drop trailing # comments and now leads with docs/tour.gif, a recorded boot showing dev/mem/ticks -- tracked (72 KB) while demos/*.gif stay ignored, since a README that shows the thing earns the bytes. Reverted two intermediate refactors that made the structure worse rather than keeping them to satisfy the checklist. Gate green: fmt clean, 21 test bins, clippy -D warnings clean on host and both bare targets, both builds ok, sw-checklist 71 passed / 0 failed / 0 warnings.