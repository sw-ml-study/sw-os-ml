Closed step 006's BootInfo gap. Kernel image extent now comes from linker symbols (__kernel_start/__kernel_end, including .bss and the boot stack) and is marked MemoryKind::Kernel; the device tree blob reserves itself during probe as Reclaimable. Boot reports 510 MiB usable across five regions instead of 512 across one. Restructuring was needed as predicted: mlos-hal-aarch64 was at the 4-module gate, so the walker moved to a new architecture-neutral mlos-machine crate (regions/reserve/scan), leaving mlos-hal-aarch64 with entry, console and image extent. reserve() is a pure free function returning a new map, so it is host-testable with no VM: 5 tests cover three-way splitting, byte conservation, exact/disjoint edges, spanning regions, and a full map refusing rather than panicking. CORRECTION to my step-005 report: I misread the blob's totalsize as 4096 from the QEMU monitor's little-endian display; FDT headers are big-endian and it is 1 MiB, so the 1 MiB Reclaimable region is correct -- recorded in docs/status.md as worth reclaiming. Retired 4 sw-checklist warnings in-step. Gate green: fmt clean, 18 tests, clippy -D warnings clean on host and both bare targets, both builds ok, sw-checklist 41 passed / 0 failed / 0 warnings.