mlos-fdt (bounds-checked FDT walker: cursor, header, reg-cell decoding, Event-based walk, no allocator, architecture-neutral) plus devicetree.rs in mlos-hal-aarch64 producing a Machine, which the kernel turns into BootInfo. Kernel prints a discovered machine: dtb pointer, pl011 base, cpu count, usable bytes, memory regions -- verified to track QEMU flags (-smp 4 -m 1024 reports 4 cpus / 1024 MiB). Deleted step 004's early_console() crutch entirely; the console address now comes from the pl011@ node, and a failed probe parks rather than guessing. Tests run against a real captured QEMU DTB (8588 bytes, trimmed from dumpdtb's 1 MiB padding), including corruption tests for magic/version/truncation, since the blob is the first untrusted input the kernel sees. Retired 2 sw-checklist warnings in-step (devicetree.rs 5 functions -> 4; mlos_main 28 lines -> banner module). Clippy correctly flagged mlos_main as dereferencing a raw pointer without an unsafe signature; now unsafe extern "C", matching the # Safety section it already had. KNOWN GAP recorded in docs/status.md: BootInfo marks the whole DRAM region Usable including the kernel image, so usable_bytes() overstates; nothing consumes it yet, 008-mmu closes it. Gate green: fmt clean, 13 tests, clippy -D warnings clean on host and both bare targets, both builds ok, sw-checklist 31 passed / 0 failed / 0 warnings.