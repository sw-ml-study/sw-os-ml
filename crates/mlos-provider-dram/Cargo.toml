[package]
name        = "mlos-provider-dram"
version     = "0.0.1"
description = "The resident tier: objects already in memory."
edition.workspace      = true
rust-version.workspace = true
license.workspace      = true
repository.workspace   = true
authors.workspace      = true

[dependencies]
mlos-abi      = { path = "../mlos-abi",      version = "0.0.1" }
mlos-objtab   = { path = "../mlos-objtab",   version = "0.0.1" }
mlos-provider = { path = "../mlos-provider", version = "0.0.1" }

[lints]
workspace = true
