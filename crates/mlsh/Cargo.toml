[package]
name        = "mlsh"
version     = "0.0.1"
description = "The MLOS inspector shell: a line reader and a few verbs."
edition.workspace      = true
rust-version.workspace = true
license.workspace      = true
repository.workspace   = true
authors.workspace      = true

[dependencies]
mlos-abi   = { path = "../mlos-abi",   version = "0.0.1" }
mlos-hal   = { path = "../mlos-hal",   version = "0.0.1" }
mlos-line  = { path = "../mlos-line",  version = "0.0.1" }
mlos-objman = { path = "../mlos-objman", version = "0.0.1" }
mlos-objtab = { path = "../mlos-objtab", version = "0.0.1" }
mlos-metrics = { path = "../mlos-metrics", version = "0.0.1" }
mlos-queue = { path = "../mlos-queue", version = "0.0.1" }
mlos-synth = { path = "../mlos-synth", version = "0.0.1" }

[lints]
workspace = true
