[package]
name        = "mlos-console"
version     = "0.0.1"
description = "Choosing a console: a PL011, or a virtio console where there is none."
edition.workspace      = true
rust-version.workspace = true
license.workspace      = true
repository.workspace   = true
authors.workspace      = true

[dependencies]
mlos-device  = { path = "../mlos-device",  version = "0.0.1" }
mlos-machine = { path = "../mlos-machine", version = "0.0.1" }
mlos-pl011   = { path = "../mlos-pl011",   version = "0.0.1" }
mlos-virtio  = { path = "../mlos-virtio",  version = "0.0.1" }

[lints]
workspace = true
