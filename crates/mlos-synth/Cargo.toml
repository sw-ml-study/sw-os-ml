[package]
name        = "mlos-synth"
version     = "0.0.1"
description = "A synthetic transformer's worth of ML objects, to exercise the manager."
edition.workspace      = true
rust-version.workspace = true
license.workspace      = true
repository.workspace   = true
authors.workspace      = true

[dependencies]
mlos-abi      = { path = "../mlos-abi",      version = "0.0.1" }
mlos-objtab   = { path = "../mlos-objtab",   version = "0.0.1" }
mlos-provider = { path = "../mlos-provider", version = "0.0.1" }
mlos-virtio     = { path = "../mlos-virtio",     version = "0.0.1" }
mlos-virtio-blk = { path = "../mlos-virtio-blk", version = "0.0.1" }

[lints]
workspace = true
